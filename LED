nodemcu 的板载LED的引脚是2号
循环点亮LED程序：
from machine import Pin
import time
def flash_led(gpio_led):
    i=0
    p0 = Pin(gpio_led, Pin.OUT)
    while i<10:
        i=i+1
        p0.value(1)
        time.sleep_ms(300)
        p0.value(0)
        time.sleep_ms(300)
flash_led(2)
